const newEndpoint = "/new";
const loginEndpoint = "/login";
const meEndpoint = "/me";
const updateEndpoint = "/update";

module.exports = {
    newEndpoint,
    loginEndpoint,
    meEndpoint,
    updateEndpoint
}